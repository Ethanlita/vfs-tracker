# 管理列表筛选请求隔离（#132）

事件与嗓音测试列表为当前查询维护单一请求代次。完整读取开始时创建新代次；状态、类型、搜索和清除搜索在用户操作的同一事件中立即使旧代次失效。分页请求继承发起时的代次，因此后到的旧响应不能再追加到新筛选列表，也不能改写新筛选的分页游标、加载状态或错误。

失效检查覆盖成功、失败和 finally 清理路径。新完整查询会同步结束旧的“加载更多”展示并清理对应错误，避免失效请求留下永久忙碌状态。用户信息缓存仍按 userId 隔离，不参与列表和游标归属判断。

5 项可控延迟测试通过，覆盖事件状态、事件类型、事件搜索、测试状态和测试搜索；每项均执行“旧分页等待 → 新查询完成 → 旧分页释放”，并验证旧记录与旧游标均未出现。隔离生产构建通过。开发/生产 × 390/1440px 四组 Chromium 验收分别对事件已通过和测试已完成筛选释放后到分页，每组最终各保留 1 条匹配记录、0 条旧记录和 0 个旧游标；手机和桌面截图已检查。

加入该修复后的完整单元测试为 82 个文件、1231 项，全部通过。
