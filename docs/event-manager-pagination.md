# 事件管理分页与搜索

事件管理每页挂载最多20张卡片（#142），复用现有分页组件。分页仅影响展示，搜索仍遍历全部已读取记录，不通过截断历史来掩盖渲染成本。

日期与搜索文本在事件数据变化时预计算，排序仅依赖事件集合与排序方式，输入搜索词不再重复解析日期和排序。切换搜索、类型、时间范围和排序回到第一页；翻页重置列表滚动位置。删除末页最后一条时页码收敛到有效范围，删除目标仍由事件ID确定。

25项相关组件/集成测试通过，覆盖25/250/1000条的挂载上限、跨页搜索、清空回首页、范围边界及删除竞态。生产构建通过。后端历史分页完整性属于#116，本次不改变请求数量或后端读取范围。

尚未提交或部署。

2026-09-11浏览器验收：开发/生产×390/1440px四组通过，25→250→1000→25条每组输入a/u/d及三次退格，始终挂载20条且不新增GET。1000条可翻到末页查看第999号样本并准确删除，剩余末页19条；搜索第0号样本可达，清空恢复首页。生产手机截图已检查。

同样4倍CPU降速、input到两次rAF的实验：1000条生产手机33.8–38.0ms，桌面36.6–43.5ms；原审查手机为692–1022ms。追加生产390px长任务观察：25/250/1000/25条每组六次输入，未观察到50ms以上长任务，1000条36.4–39.2ms。开发未降速的1000条约4.8–7.2ms。以上是本机合成数据实验，不是INP或真实用户p75；完整原始数组在 `manager-pagination-browser.log` 与 `manager-pagination-longtasks.log`，历史对照见issue草稿。
