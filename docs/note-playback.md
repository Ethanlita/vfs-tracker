# Hz音符工具播放

StrictMode的每次effect设置都会恢复挂载标记，表单提交不再被模拟卸载误拦截。卸载使待播放请求失效、关闭上下文并清空音色引用。

每个上下文最多有一个音色加载请求。加载期间按键立即使用合成器，不排队等待下载。音色完成后仅用于新的按键，状态栏明确显示加载与实际音色；失败后继续使用合成器，断网再恢复允许新按键重试下载。离开后忽略迟到的加载结果，不在已关闭上下文播放。

组件测试覆盖StrictMode转换、慢成功/失败、共享请求、无补播、网络恢复与卸载后重新进入。

#138的范围一致性已修复：音名等式使用夹取后的琴键，与输入框、频率和高亮一致。原输入单独展示，例如C0显示A0=27.50Hz，C9显示C8=4186.01Hz。升降号保持规范化，非法输入不触发播放。

2026-09-11：4项组件测试、生产构建通过。开发/生产浏览器挂起真实音色请求，表单220Hz→A3、连续白键点击立即发声，请求总数1；失败不补播，Space触发新音正常。手机白键测试点击其下部可见区域，中心可能被黑键覆盖。已查看加载状态截图；慢成功的真实音色播放仍待浏览器补验。

后续验收：真实音色慢成功已在开发/生产验证，通过下载完成时不补播，新Space按键各启动1个钢琴采样音源；计数排除ADSR的循环电压节点。12项组件测试及最新生产构建通过，开发/生产×390/1440px共24次边界/升降号转换通过，已检查手机结果截图。
