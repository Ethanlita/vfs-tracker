# 声音图表的独立指标

声音频率图表按当前选择的指标读取有效有限数值（#130），不再要求其他指标必须同时有基频。缺失、null、非数值不会进入图表；0参与统计。日期排序和时间范围仍使用统一事件日期逻辑。

Shimmer按现有输入表单、详情和Schema的百分比显示（#131），不做数值换算。图表坐标轴、最新值与平均值共用指标单位；Jitter为%、HNR为dB、基频为Hz。图表启用采样点，单条记录也可见。

合法混合fixture `metric-events.js`包含无基频的Jitter/Shimmer/HNR、HNR零值和乱序日期。组件测试覆盖各指标最新值/均值、缺失空态与非法输入。8项相关测试、1073项全量单元测试和生产构建通过。

关联待查：SelfTestDetails/HospitalTestDetails部分指标卡片仍用真假值判断，可能隐藏零值；本次图表修复不能证明详情的零值显示已经正确。

尚未提交或部署。

2026-09-11：开发/生产×390/1440px共4组、16次真实图表检查通过：数据点数1/2/1/2、最新值及平均值、坐标轴单位均正确；Shimmer单点3.2%可见，平均3.20%。已查看生产手机截图。测试用合成账号和模拟事件读取，未访问真实数据。日志/截图为 output/playwright/frontend-audit/chart-metrics-*。

后续核对：详情卡片零值问题已登记#175并完成本地修复与双模式验证，见[详情零值说明](event-detail-zero.md)。
