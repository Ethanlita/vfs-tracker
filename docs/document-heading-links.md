# 文档目录与章节链接

PostViewer使用rehype-slug生成GitHub风格标题ID（#136），中文保留、标点按slug规则处理，重复标题获得稳定后缀。生成规则作用于全部Markdown文档，不单独硬编码某篇目录。

正文异步加载完成以及hash变化时，在当前文章范围内寻找对应标题。只定位标题节点，错误的百分号编码或不存在的目标安全忽略。目标滚动时保留6rem顶部间距，并获得键盘焦点，便于从章节继续阅读。事件监听在卸载和内容变化时清理。

测试覆盖中文标点、格式化标题文字、重复标题、慢加载深链接和无效hash；结合文档读取测试共8项通过。生产构建通过。没有启用Markdown原始HTML。

尚未提交或部署。

2026-09-11：开发/生产×390/1440px四组浏览器验证通过，每组键盘激活实际文章全部4个目录项、直接打开编码hash以及刷新。目标焦点正确且不被固定导航遮挡；坐标检查等待平滑滚动到位。已检查生产手机参考文献截图。证据为 output/playwright/frontend-audit/docs-anchor-*。
