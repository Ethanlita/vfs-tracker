# 个人历史读取状态

MyPage的历史内容区域按加载、失败、成功互斥展示（#163）。首次异步读取和重试期间显示状态提示；失败显示“无法加载历史记录”与ApiErrorNotice重试，不渲染图表或首次记录引导。

只有成功取得空数组才显示“暂无事件”。有记录但没有选定指标时，时间轴显示记录，图表可以显示该指标空态。请求结果直接作为唯一数据来源，移除复制事件状态；结果绑定账号ID，重试不保留旧统计冒充最新结果。成功清除错误并显示新的统计和时间轴。

33项个人页集成测试及生产构建通过，测试覆盖错误/慢重试不显示误导空态、成功事件恢复及原有导航行为。尚未提交或部署。

2026-09-11：开发/生产×390/1440px四组浏览器流程通过，包含400、500、网络失败、慢重试、180Hz恢复、成功空列表以及只有感受记录而无指标。失败和等待期间没有无记录文案，手动重试仅新增一次读取；成功无指标仍保留事件。合成账号与模拟API，无真实数据读写。已查看生产手机错误页截图，证据为 output/playwright/frontend-audit/history-status-*。
