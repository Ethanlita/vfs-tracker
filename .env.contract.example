# 环境配置示例 - 契约测试（真实 API）
# ⚠️ 重要：这些需要填入真实的 AWS 凭证
# 请复制此文件为 .env.contract 并填入实际值

# Cognito User Pool ID (真实的)
VITE_COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX

# Cognito App Client ID (真实的)
VITE_COGNITO_USER_POOL_WEB_CLIENT_ID=XXXXXXXXXXXXXXXXXXXXXXXXXX

# AWS 区域
VITE_AWS_REGION=us-east-1

# API Gateway 端点 (真实的)
VITE_API_ENDPOINT=https://XXXXXXXXXX.execute-api.us-east-1.amazonaws.com/dev

# S3 存储桶名称 (真实的)
VITE_S3_BUCKET=vfs-tracker-bucket-XXXXXX

# 测试用户凭证（可选，用于需要认证的测试）
# TEST_USER_EMAIL=test@example.com
# TEST_USER_PASSWORD=YourTestPassword123!
