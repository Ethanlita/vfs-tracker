# 契约测试环境变量配置示例
# Contract Test Environment Variables Example
#
# 使用方法 / Usage:
# 1. 复制此文件为 .env.contract
# 2. 填入真实的 AWS 凭证
# 3. .env.contract 已在 .gitignore 中，不会被提交
#
# ⚠️ 警告 / WARNING:
# - 这些是真实的 AWS 环境变量，不是占位符！
# - 不要将 .env.contract 提交到 Git！
# - These are REAL AWS environment variables, not placeholders!
# - DO NOT commit .env.contract to Git!

# ====================
# AWS Cognito 配置 / AWS Cognito Configuration
# ====================

# Cognito 用户池 ID / Cognito User Pool ID
# 格式: us-east-1_XXXXXXXXX
VITE_COGNITO_USER_POOL_ID=us-east-1_YourPoolId

# Cognito 用户池客户端 ID / Cognito User Pool Client ID  
# 格式: 26个字符的字母数字字符串
VITE_COGNITO_USER_POOL_WEB_CLIENT_ID=YourClientIdHere1234567890

# AWS 区域 / AWS Region
VITE_AWS_REGION=us-east-1

# ====================
# API 配置 / API Configuration
# ====================

# API Gateway 端点 / API Gateway Endpoint
# 格式: https://<api-id>.execute-api.<region>.amazonaws.com/<stage>
VITE_API_ENDPOINT=https://xxxxxxxxxx.execute-api.us-east-1.amazonaws.com/dev

# API 阶段 / API Stage (通常是 dev 或 prod)
VITE_API_STAGE=dev

# ====================
# S3 配置 / S3 Configuration
# ====================

# S3 存储桶名称 / S3 Bucket Name
VITE_S3_BUCKET=your-vfs-tracker-bucket-name

# ====================
# 测试用户凭证 / Test User Credentials
# ====================

# 测试用户邮箱 / Test User Email
# ⚠️ 必须是真实存在于 Cognito 用户池中的用户
# ⚠️ Must be a real user in your Cognito User Pool
TEST_USER_EMAIL=test@example.com

# 测试用户密码 / Test User Password
# ⚠️ 保持密码安全，不要在代码或日志中暴露
# ⚠️ Keep password secure, never expose in code or logs
TEST_USER_PASSWORD=YourSecurePassword123!

# ====================
# 如何获取这些值 / How to Get These Values
# ====================
#
# 1. Cognito User Pool ID 和 Client ID:
#    - 登录 AWS Console
#    - 进入 Amazon Cognito
#    - 选择你的用户池
#    - User Pool ID 在 "General settings" 页面
#    - Client ID 在 "App clients" 页面
#
# 2. API Endpoint:
#    - 登录 AWS Console
#    - 进入 API Gateway
#    - 选择你的 API
#    - 在 "Stages" 中找到你的阶段 (如 dev)
#    - "Invoke URL" 就是你的 API Endpoint
#
# 3. S3 Bucket:
#    - 登录 AWS Console  
#    - 进入 S3
#    - 找到你的 VFS Tracker 存储桶名称
#
# 4. 测试用户:
#    - 在 Cognito User Pool 中创建一个专门用于测试的用户
#    - 或使用现有的测试账号
#    - 确保该用户有创建/读取/更新/删除事件的权限

# ====================
# 验证配置 / Verify Configuration
# ====================
#
# 配置完成后，运行以下命令验证:
# After configuration, run the following command to verify:
#
#   npm run test:contract
#
# 如果所有测试通过，说明配置正确！
# If all tests pass, your configuration is correct!

