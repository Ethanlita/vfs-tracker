# AWS Amplify 配置
# 请复制此文件为 .env 并填入实际的AWS配置值

# AWS 区域和认证配置
VITE_AWS_REGION=us-east-1
VITE_COGNITO_USER_POOL_ID=your_user_pool_id_here
VITE_COGNITO_USER_POOL_WEB_CLIENT_ID=your_web_client_id_here

# API Gateway 配置
VITE_API_ENDPOINT=https://your-api-id.execute-api.us-east-1.amazonaws.com
VITE_API_STAGE=dev

# S3 存储配置
VITE_S3_BUCKET=your-s3-bucket-name

# 可选：AI 功能配置
# VITE_GOOGLE_GEMINI_API=your_gemini_api_key_here

# 注意：
# 1. 以上所有配置（除可选项）都是必需的，缺少任何一个都会导致应用启动失败
# 2. 这些值需要从 AWS 控制台获取（Cognito、API Gateway、S3）
# 3. VITE_ 前缀是必需的，这样 Vite 才能在前端访问这些变量
# 4. 不要将包含真实值的 .env 文件提交到 git
