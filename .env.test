# 环境配置示例 - 集成测试（使用 MSW mock）
# 这些是假的测试值，只是为了让 isProductionReady() 返回 true
# 实际的 API 请求会被 MSW 拦截

VITE_COGNITO_USER_POOL_ID=us-east-1_TEST123456
VITE_COGNITO_USER_POOL_WEB_CLIENT_ID=test-client-id-123456
VITE_AWS_REGION=us-east-1
VITE_API_ENDPOINT=https://test-api.execute-api.us-east-1.amazonaws.com/dev
VITE_S3_BUCKET=test-vfs-tracker-bucket
